const assert = require("assert");
const fpSnake = require("../index.js");

describe("fpSnake", () => {
  it("has a test", () => {
    fpSnake.initSnakeTable();
    assert(false, "fpSnake should have a test");
  });
});
